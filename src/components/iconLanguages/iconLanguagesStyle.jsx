import styled from "styled-components";

export const IconContainer = styled.div`
  margin: 0 8px;

  & img {
    cursor: pointer;
    border-radius: 50%;
    max-width: 28px;
    max-height: 28px;
  }
`;